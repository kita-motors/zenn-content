# zenn-drafts

Zenn（zenn.dev/kitamotors）の記事リポジトリのローカル。GitHub: `kita-motors/zenn-content`（Public）と双方向同期。push すると Zenn が自動デプロイする。

`zenn-drafter` サブエージェント経由で書く場合も、メインから直接 `articles/` を編集する場合も、**このファイルに書かれた著者ペルソナと「絶対に守ること」を必ず先に踏まえること**。

## 著者ペルソナ

- 一人称「私」
- **中小企業の事務・管理部門に所属する非エンジニア**。プログラミングは目的ではなく、現場の困りごとを減らすための手段
- 業務システムを内製している実践者。Claude Code / 自作ツール（kita-memory など）を「非エンジニアの実装パートナー」として使いこなしている
- 口調: です・ます調。丁寧だが堅すぎず、技術ブログとして読みやすく
- 差別化軸: **「エンジニアではない人間が AI と一緒にシステムを組んでいる」** という視点。専門家ぶった語り、エンジニア向け前提知識を当然視する書き方は避ける。「逆にここで詰まった」「専門用語の意味から確認した」のような非エンジニアならではの視点を活かす
- 二本柱: ① Claude Code / AI エージェント運用、② 業務自動化（GAS / API 連携 / ブラウザ拡張）

## 絶対に守ること（記事品質の根本）

Zenn 読者は **AI が大量生成した記事を嫌う**。技術コミュニティで「この人の記事は信用できる」と思われるかは、**一次情報の含有率**で決まる。以下を必ず守る。

### 1. 捏造しない

- 記憶（kita-memory）や現セッションで観測した事実に**書かれていないこと**を本文に入れない
- 創作してはいけないもの: コード・コマンド・エラーメッセージ・バージョン番号・**装飾的な数字**（例「6 回 × 8 時間で 48 回」）・**時系列の脚色**（例「数日経つとだんだん気になり始めた」など本人が口にしていない経過）
- **感想も創作対象**: 「画面に向かう気分が軽くなった」「目の端から保存してるぞアピールが消えた」のような体感表現は、本人が会話で実際に口にした言葉以外は書かない
- 不確かな技術情報は書かない。記憶に根拠がなければ本文から省く

### 2. AI 文体を避ける

書き終わったら以下のパターンが入っていないかセルフチェックする。入っていたら削るか、本人が実際に言った言葉に置き換える。

- **過度に意義を主張する締め**: 「〜のおかげで安全にできました」「気分が軽くなりました」など
- **計算・数字でアピール**: 「N 回 × M 時間で XX 回」など装飾的な数字
- **抽象的すぎる比喩**: 「方針を一段上げて」「ぶら下がる構造」「取り趣す」など
- **説教調まとめ**: 「教訓としては〜ということでした」
- **既存本文の文体を観察し、それに寄せる**。新規記事の場合も「私の手に余る話でした」「気休めです」「夜眠れます」のような既存記事に近いドライさを基準にする

### 3. 体験談の骨子を記憶から抽出 → 著者の口で語り直す

- AI 全文生成ではなく、**記憶にある事実を骨子にして、著者の語り口で書き直す**
- 「実装の手触り」が伝わる短い具体（失敗・原因・解決）を優先。一般論や入門的解説は不要

## 既存記事のリライト/追記

新規生成は `zenn-drafter` サブエージェントの仕事。**既存記事のリライト/追記**を行う場合は以下の順序を守る。

1. 編集対象の記事を **全文 Read で先に読む**。既存本文の文体・語彙・トーンを観察する
2. 追加する内容について「現セッションまたは kita-memory 内に一次情報があるか」を確認する。なければ書かない
3. 書き終わったら 上記「2. AI 文体を避ける」と「1. 捏造しない」でセルフチェック
4. **ファイルに書き込む前に、本文を会話で提示してユーザーレビューを受ける**（AI 文体は本人の感覚でしか弾けないため、編集前に必ず一度通す）

## 記事フォーマット

frontmatter のテンプレート:

    ---
    title: "（30〜45文字目安、検索流入と気持ちに刺さるバランス）"
    emoji: "🛠️"
    type: "tech" または "idea"
    topics: ["claudecode", "..."]
    published: false
    ---

- ファイル名（slug）: 12〜50字、英数字 + ハイフン。**日付プレフィックスは付けない**（`2026-05-12-foo` ではなく `foo`）。内容を表す英語スラッグのみ。日付はファイル名で管理しない（git 履歴と frontmatter で足りる）
  - 既存の `2026-05-13-...` 等の日付付きで**公開済み（published: true）**の記事は Zenn 上の URL がそのスラッグで確定しているため**改名しない**（URL が変わり外部リンク・SEO が切れる）。日付なしルールは新規および未公開ドラフトに適用する
- topics は最大5個
- published は **必ず false** で書き始め、レビュー後に true へ

## 公開フロー

1. `articles/<slug>.md` を作成・編集
2. `npx zenn preview` でローカル確認（http://localhost:8000）
3. `published: true` に切り替えてコミット
4. `git push origin main`（user が手元で実行）
5. Zenn 自動デプロイで数分以内に反映
6. X 投稿用テンプレを Claude Code 側で生成して、user が手動投稿

## マスキング（このリポジトリは Public）

記事・README・コミットメッセージにも以下は出さない。判断に迷ったら**伏字側に倒す**。

- 業務系の社内ツール名・コードネーム
- 顧客名・社内ドメイン・社内メアド
- API キー・トークン・パスワード様の文字列
- 個人名・社内チャットのハンドル
- 業務で使っている特定 SaaS の固有名は、文脈で個人特定に繋がりそうなら一般化（例: ジョブカン → クラウド勤怠 SaaS）

## 既出記事

- `prologue-claude-code-business-automation.md` — プロローグ宣言記事（type=idea、published）
- `kita-memory-mcp-claude-code-long-term-memory.md` — 第1弾、MCP サーバー体験談（type=idea、published、Zennfes Spring 2026 応募）

## zenn-drafter エージェント

`~/.claude/agents/zenn-drafter.md` に配置したサブエージェント。kita-memory MCP の work カテゴリ記録から技術記事下書きを生成する。明示呼び出しのみ起動。重めの作業が一区切りついたら「回しますか？」と user に提案する運用。
